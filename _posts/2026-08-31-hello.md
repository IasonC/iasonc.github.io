---
layout: post
title: A first post
date: 2026-08-31 10:00:00
description: Placeholder — delete this once you have something real to say.
published: false # keeps it out of the build; kept here as a template to copy
tags: 
categories: 
---

This file is unpublished, so it appears nowhere on the site. It's here as a template.

To add a real post: copy this into `_posts/YYYY-MM-DD-slug.md`, drop the
`published: false` line, and write. `description` is the one-line pitch shown in the
writings list. Then re-show the writings tab by setting `nav: true` in `_pages/blog.md`
and `latest_posts.enabled: true` in `_pages/about.md`.

Markdown works as you'd expect, including code:

```python
def hello(name):
    return f"hello, {name}"
```

and maths, if a post needs it: $$ \mathcal{L}(\theta) = -\frac{1}{N}\sum_{i=1}^{N} \log p_\theta(y_i \mid x_i) $$
